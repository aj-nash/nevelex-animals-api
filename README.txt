Setup Instructions for Mac OSX:

Install pip3 if not already.

Open a terminal and run:
$ pip3 install flask
$ pip3 install requests

cd into the project directory

run
$ python3 flask_main.py

Copy the terminal-listed http://IP_AND_PORT_NUMBER into the browser bar to access and use the app.